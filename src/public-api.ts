/**
 * Public API Surface of azure-devops-npm-registry-auth.
 * Although this package is only intended to be used via CLI, we expose a public API surface for our tests only.
 */
export * from "./cli";
